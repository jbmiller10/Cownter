"""Herd app configuration."""

from django.apps import AppConfig


class HerdConfig(AppConfig):
    """Configuration for the herd app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "herd"
